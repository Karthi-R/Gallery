<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.custom.library">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity android:name=".TextEdit.TextEditorActivity"></activity>
        <activity android:name=".Adjustment.AdjustmentActivity" />
        <activity android:name=".ui.ImageEditActivity" />
        <activity android:name=".ui.SaveEditedImage" />
        <activity
            android:name=".ui.CropActivity"
            android:theme="@style/CropTheme" />
        <activity android:name=".ui.ShadowActivity" />
        <activity
            android:name=".ui.ImageGridActivity"
            android:theme="@style/ImagePickerTheme" />
        <activity
            android:name=".ui.ImagePreviewActivity"
            android:theme="@style/ImagePickerTheme" />
        <activity
            android:name=".ui.ImagePreviewDelActivity"
            android:theme="@style/ImagePickerTheme" />
        <activity
            android:name=".ui.ImageCropActivity"
            android:theme="@style/ImagePickerTheme" />
        <activity android:name=".ui.FolderGridActivity"
            android:theme="@style/ImageSelectionTheme"/>
        <activity android:name=".Crop.CropImageActivity" />
        <activity android:name=".Edit.CropImageActivity" />
        <activity android:name=".ui.ImagePagerActivity" />

        <provider
            android:name=".ImagePickerProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

</manifest>