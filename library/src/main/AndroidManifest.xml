<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.custom.library">
    <!-- 往SDCard写入数据权限 -->
    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity android:name="com.custom.library.ui.SaveEditedImage"></activity>
        <activity
            android:name="com.custom.library.ui.CropActivity"
            android:theme="@style/CropTheme" />
        <activity android:name="com.custom.library.ui.ShadowActivity" />
        <activity
            android:name="com.custom.library.ui.ImageGridActivity"
            android:theme="@style/ImagePickerTheme" />
        <activity
            android:name="com.custom.library.ui.ImagePreviewActivity"
            android:theme="@style/ImagePickerTheme" />
        <activity
            android:name="com.custom.library.ui.ImagePreviewDelActivity"
            android:theme="@style/ImagePickerTheme" />
        <activity
            android:name="com.custom.library.ui.ImageCropActivity"
            android:theme="@style/ImagePickerTheme" />
        <activity android:name="com.custom.library.ui.FolderGridActivity" />
        <activity android:name="com.custom.library.Crop.CropImageActivity" />
        <activity android:name="com.custom.library.Edit.CropImageActivity" />
        <activity android:name="com.custom.library.ui.ImagePagerActivity" />

        <provider
            android:name="com.custom.library.ImagePickerProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application> <!-- 从SDCard读取数据权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>